<div class="container">

<% if (typeof errors !="undefined" ) { %>
  <ul>
    <%errors.forEach(error=> { %>
      <li class="error">
        <%= error.msg %>
      </li>
      <% }) %>
  </ul>
  <% } %>

</div>